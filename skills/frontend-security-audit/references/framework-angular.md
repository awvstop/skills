# Angular / AngularJS — v6

**Angular(2+)：** bypassSecurityTrustHtml/ResourceUrl/Style/Script/Url()每处验证 · [innerHTML]+bypass跳过净化 · [src][href][style]绑定不可信 · Universal SSR注入 · HttpClient拦截器中URL拼接

**AngularJS(1.x)：** ng-bind-html无$sanitize · $sce.trustAsHtml/Url/Js() · 表达式注入{{用户输入}} · ng-include="用户输入" · $compile(用户HTML)(scope) · $http URL拼接
